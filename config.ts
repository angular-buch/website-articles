/**
 * Build configuration for website-articles.
 * This file is in the repo root so the build folder can be synced independently.
 */

/** Base URL for markdown assets (images, etc.) */
export const MARKDOWN_BASE_URL = 'https://website-articles.angular-buch.com/';

/** Output folder for built files (relative to build folder) */
export const DIST_FOLDER = './dist';

/** Source folder for blog posts (relative to build folder) */
export const BLOG_POSTS_FOLDER = '../blog';
